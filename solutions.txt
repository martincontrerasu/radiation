db = odbc.odbc('Driver={Microsoft Access Driver (*.mdb)};Dbq=Nwind.mdb;Uid=;Pwd=;')

https://pypi.org/project/pandas_access/

sources = pyodbc.dataSources()
keys = sources.keys()
for key in keys: 
   print key
   
  
https://stackoverflow.com/questions/5402463/how-do-i-access-a-mdb-file-from-python


import pyodbc

db_file = r'''/path/to/the/file.res'''
user = 'admin'
password = 'password'
odbc_conn_str = 'DRIVER={Microsoft Access Driver (*.mdb)};DBQ=%s;UID=%s;PWD=%s' % (db_file, user, password)

conn = pyodbc.connect(odbc_conn_str)

cursor = conn.cursor()

cursor.execute("select * from table order by some_column")

for row in cursor.fetchall():
    print ", ".join((row.column1, row.column2, row.columnN))
